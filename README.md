# AuConfortoSite

Site/app AuConforto (Vite + React). Branch principal de desenvolvimento: **`main`**.

## Branches

| Branch | O quê |
|---|---|
| `main` | App/site (esta branch) |
| `shopify` | Tema Shopify da loja AuConforto (arquivos do tema na raiz + `VERSION`/`CHANGELOG.md` versionados por tag) |

Ver `BRANCHES.md` na branch `shopify` para a convenção de versionamento do tema.
